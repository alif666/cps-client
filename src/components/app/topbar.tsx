'use client';
import { Bell, Menu } from 'lucide-react';
import { useSession } from '@/lib/auth/session-context';
export function Topbar({ title, eyebrow }: { title: string; eyebrow: string }) { const { user } = useSession(); return <header className="flex min-h-20 items-center justify-between border-b border-slate-200 bg-white/80 px-5 backdrop-blur sm:px-8"><div className="flex items-center gap-3"><button className="rounded-lg p-2 text-slate hover:bg-cloud lg:hidden" aria-label="Open navigation"><Menu className="h-5 w-5" /></button><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand">{eyebrow}</p><h1 className="mt-1 text-xl font-semibold tracking-tight text-ink">{title}</h1></div></div><div className="flex items-center gap-4"><button className="relative rounded-xl border border-slate-200 p-2.5 text-slate transition hover:border-brand/30 hover:text-brand" aria-label="Notifications"><Bell className="h-4 w-4" /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-accent" /></button><div className="hidden text-right sm:block"><p className="text-sm font-semibold text-ink">{user?.fullName ?? 'Workspace user'}</p><p className="text-xs text-slate">{user?.roles?.[0]?.code?.replaceAll('_', ' ') ?? 'Authorized user'}</p></div><div className="grid h-10 w-10 place-items-center rounded-full bg-brand-soft text-sm font-bold text-brand">{user?.fullName?.slice(0, 1).toUpperCase() ?? 'U'}</div></div></header>; }
